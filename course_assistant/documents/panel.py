"""Documents tab (owner: Chase). Placeholder until the real panel is built."""

import gradio as gr


def build_panel() -> None:
    """Add this tab's components. Called by app.py inside a gr.Tab."""
    gr.Markdown("**Documents**: coming soon. Upload, list, and remove course files.")
