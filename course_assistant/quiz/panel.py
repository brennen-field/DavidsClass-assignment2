"""Quiz tab (owner: Brennen). Placeholder until the real panel is built."""

import gradio as gr


def build_panel() -> None:
    """Add this tab's components. Called by app.py inside a gr.Tab."""
    gr.Markdown("**Quiz**: coming soon. Pick material and an optional topic, then take a quiz.")
