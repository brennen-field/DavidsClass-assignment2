"""Ask tab (owner: Preston). Placeholder until the real panel is built."""

import gradio as gr


def build_panel() -> None:
    """Add this tab's components. Called by app.py inside a gr.Tab."""
    gr.Markdown("**Ask**: coming soon. Ask a question and see the answer with sources.")
